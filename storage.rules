rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Require login for all reads/writes
      allow read, write: if request.auth != null;
    }
  }
}