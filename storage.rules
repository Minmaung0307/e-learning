rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads/writes only for signed-in users (tighten later)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}