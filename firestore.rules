rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads/writes only for signed-in users everywhere (tighten later)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}